---

---

<section
  id="cta-section"
  class="cta-section relative flex flex-col items-center justify-center py-20 text-center overflow-hidden"
>
  <!-- Background glow -->
  <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
    <div class="w-[600px] h-[600px] bg-primary/5 rounded-full blur-[120px]"></div>
  </div>

  <div class="relative z-10">
    <p class="text-sm font-semibold uppercase tracking-widest text-primary mb-4">ابدأ الآن</p>
    <h2 id="cta-title" class="mb-4 text-4xl font-bold font-serif md:text-6xl gradient-text-primary">
      نشاطك يستاهل تطبيق
    </h2>

    <p id="cta-subtitle" class="mb-14 text-xl md:text-2xl text-neutral font-medium max-w-xl mx-auto leading-relaxed">
      كلمنا واتساب وخلال 14 يوم تطبيقك يكون جاهز
    </p>

    <div class="bg-white/5 backdrop-blur-sm rounded-3xl p-10 md:p-14 shadow-2xl border border-white/10 mx-auto max-w-md mb-14">
      <div class="space-y-5">
        <p id="cta-mvp-text" class="text-lg md:text-xl text-neutral font-medium">
          تطبيق + لوحة تحكم
        </p>
        <div class="flex items-baseline justify-center gap-2 text-primary tracking-tight">
          <span id="cta-price-from" class="text-4xl md:text-5xl font-bold">5,600</span>
          <span class="text-2xl md:text-3xl text-neutral/50">—</span>
          <span id="cta-price-to" class="text-4xl md:text-5xl font-bold">9,600</span>
          <span id="cta-price-currency" class="text-xl md:text-2xl text-neutral mr-1">ريال</span>
        </div>
        <p id="cta-price-note" class="text-sm md:text-base text-neutral/70 leading-relaxed max-w-sm mx-auto">
          السعر يعتمد على حجم المشروع والمميزات المطلوبة
        </p>
      </div>
    </div>

    <a
      id="cta-button"
      href="https://wa.me/966563913944?text=مرحباً، أبغى تطبيق لنشاطي وأبغى أعرف التفاصيل."
      target="_blank"
      rel="noopener noreferrer"
      class="inline-flex items-center justify-center gap-3 rounded-full bg-gradient-to-r from-indigo-600 via-violet-600 to-purple-600 px-12 py-5 transition-all duration-300 hover:scale-105 shadow-2xl hover:shadow-indigo-500/40"
    >
      <span id="cta-button-text" class="text-xl font-bold text-white"
        >اطلب تطبيقك الآن</span
      >
    </a>
  </div>
</section>

<script>
  import { languageStore } from "@app/language-store";

  const updateCTA = () => {
    const ui = languageStore.content.ui;
    const pricing = languageStore.content.pricing;

    const ctaTitleEl = document.getElementById("cta-title");
    const ctaSubtitleEl = document.getElementById("cta-subtitle");
    const ctaButtonTextEl = document.getElementById("cta-button-text");
    const ctaMvpTextEl = document.getElementById("cta-mvp-text");
    const ctaPriceFromEl = document.getElementById("cta-price-from");
    const ctaPriceToEl = document.getElementById("cta-price-to");
    const ctaPriceCurrencyEl = document.getElementById("cta-price-currency");
    const ctaPriceNoteEl = document.getElementById("cta-price-note");

    if (ctaTitleEl) ctaTitleEl.textContent = ui.ctaFinalTitle || ui.ctaMain || "Ready to Launch?";
    if (ctaSubtitleEl) ctaSubtitleEl.textContent = ui.ctaSubtitle || "Let's turn your idea into an app people love";
    if (ctaButtonTextEl) ctaButtonTextEl.textContent = ui.ctaFinalButton || ui.whatsapp || "Message Us on WhatsApp";
    if (ctaMvpTextEl) ctaMvpTextEl.textContent = pricing.mvpText;
    if (ctaPriceFromEl) ctaPriceFromEl.textContent = pricing.priceFrom;
    if (ctaPriceToEl) ctaPriceToEl.textContent = pricing.priceTo;
    if (ctaPriceCurrencyEl) ctaPriceCurrencyEl.textContent = pricing.priceCurrency;
    if (ctaPriceNoteEl) ctaPriceNoteEl.textContent = pricing.note;
  };

  window.addEventListener("language-change", updateCTA);
  updateCTA();
</script>
